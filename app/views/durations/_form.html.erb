<%= form_for(duration) do |f| %>
  <% if duration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(duration.errors.count, "error") %> prohibited this duration from being saved:</h2>

      <ul>
      <% duration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_date %>
    <%= f.date_select :first_date %>
  </div>

  <div class="field">
    <%= f.label :last_date %>
    <%= f.date_select :last_date %>
  </div>

  <div class="field">
    <%= f.label :duration_type %>
    <%= f.text_field :duration_type %>
  </div>

  <div class="field">
    <%= f.label :leave_request_id %>
    <%= f.number_field :leave_request_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
